services:
  cobalt:
    image: ghcr.io/imputnet/cobalt:11
    container_name: mymusic-cobalt
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - CORS_WILDCARD=1
      - LISTEN_PORT=8080
